mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  application:
    name: system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tn_content?useSSL=false
    username: root
    password: A10251123a
server:
  servlet:
    context-path: /system
  port: 7890

