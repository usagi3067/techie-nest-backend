spring:
  application:
    name: search
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: dev
        group: techie-nest-backend
      config:
        namespace: dev
        group: techie-nest-backend
        file-extension: yml